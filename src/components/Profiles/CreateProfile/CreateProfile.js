import styles from './CreateProfile.module.css'
import React from 'react'

function CreateProfile() {
  return (
    <section className={`${styles.create_profile}`}>
      
    </section>
  )
}

export default CreateProfile
